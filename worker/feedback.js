// 心得回饋：填寫頁、維護介面、上傳、推薦。
//
// 跟邀請卡完全分開的一份試算表（FEEDBACK_SHEET_ID）和
// 一個雲端資料夾（FEEDBACK_FOLDER），只有 Google 的存取層是共用的。
//
// 這裡的資料比邀請卡敏感得多——是信徒自己寫的心得。
// 教會只是保存；真的要用一定會先找本人談。程式不做「同意書打勾」那套，
// 因為打勾不等於同意，那是人跟人之間的事

import { readSheet } from "./google.js";
import { 代入 } from "./lib.js";

const 分頁 = { 回饋: "回饋單", 推薦: "推薦", 設定: "設定檔" };

// 設定檔分頁的預設值。沒建分頁、或某一則沒填，就用這裡的
const 預設文案 = {
  頁面標題: "心得回饋",
  送出按鈕: "送出",
  暫存按鈕: "先儲存",
  暫存提示: "已經幫你存起來了，之後回到這個連結還會在",
  完成訊息: "收到了，謝謝你願意寫下來　🙏",
  保存說明: "教會只是幫你保存。將來若有機會用到，一定會先跟你本人聯絡、取得你的同意。",
  上傳說明: "可以附上文件、照片或影片",
  單檔上限MB: "200",
};

export function 文案(來源, 代號, 變數) {
  const 樣板 = (來源 && 來源[代號]) || 預設文案[代號] || "";
  return 代入(樣板, 變數 || {});
}

/* ── 路由 ────────────────────────────────────────
   路徑已經去掉開頭的 f/。回 null 表示「不是我的」，
   讓 index.js 去給 404
   ──────────────────────────────────────────────── */

export async function 回饋路由(路徑, request, url, env, ctx) {
  // 還沒開工。填寫頁、/f/admin、/f/api/… 會陸續接在這裡
  return null;
}
